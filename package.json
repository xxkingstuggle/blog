{
  "name": "planetary-pulsar",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-redirects.mjs && node scripts/write-build-manifest.mjs",
    "check": "astro check && tsc --noEmit",
    "test": "tsx --test tests/**/*.test.ts",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/mdx": "^8.0.0",
    "astro": "^7.3.1",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.13",
    "wrangler": "^4.129.0"
  },
  "allowScripts": {
    "esbuild": true
  }
}
